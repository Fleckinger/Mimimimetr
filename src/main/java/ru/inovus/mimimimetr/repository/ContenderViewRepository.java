package ru.inovus.mimimimetr.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import ru.inovus.mimimimetr.entity.contender.ContenderView;

public interface ContenderViewRepository extends JpaRepository<ContenderView, Long> {
}
